import { StyleSheet } from 'react-native';
export const styles = StyleSheet.create({
  textOption: {
    color: '#666',
    fontWeight: '600',
    fontSize: 16,
  },
  textValue: {
    color: '#666',
    fontWeight: '400',
    fontSize: 14,
    alignItems: 'center',
  },
  box: {
    flexDirection: 'row',
    gap: 8,
    alignItems: 'center',
  },
});
